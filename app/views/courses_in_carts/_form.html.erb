<%= form_with(model: courses_in_cart, local: true) do |form| %>
  <% if courses_in_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(courses_in_cart.errors.count, "error") %> prohibited this courses_in_cart from being saved:</h2>

      <ul>
        <% courses_in_cart.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :courses_id %>
    <%= form.text_field :courses_id %>
  </div>

  <div class="field">
    <%= form.label :cart %>
    <%= form.text_field :cart %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
