<h1>Editing User</h1>

<% if current_user.type == "Admin" || current_user.id == @user.id %>
  <%= render 'form', user: @user %>

  <%= link_to 'Show', @user %> |
  <%= link_to 'Back', users_path %>
<% else %>
  <h4>You do not have permission to view this page</h4>
  <%= link_to 'Home', welcome_path %>
<% end %>
